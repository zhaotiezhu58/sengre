server:
  undertow:
    io-threads: 8
    worker-threads: 64
    buffer-size: 1024
spring:
  application:
    name: sengreapi
  cloud:
    nacos:
      discovery:
        server-addr: 8.218.97.208:8848
        namespace: 1a2201f4-4596-43d2-88cc-6473251242c9
      config:
        server-addr: ${spring.cloud.nacos.discovery.server-addr}
        namespace: ${spring.cloud.nacos.discovery.namespace}
        file-extension: yml
        shared-configs:
          - sharedConfiguration-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension}
  profiles:
    active: test
  main:
    allow-bean-definition-overriding: true

